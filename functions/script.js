// Functions 

let int = 5;

// Declaring a function

function showMessage() {
    if (int >= 10) {
        document.getElementById('change').innerHTML = "more than or equal to 10";
    } else {
        document.getElementById('change').innerHTML = "less than or equal to 10";
    }
}
